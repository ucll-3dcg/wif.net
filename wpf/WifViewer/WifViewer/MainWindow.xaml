<Window x:Class="WifViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WifViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <DockPanel>
        <Slider DockPanel.Dock="Top" Value="{Binding CurrentImageIndex.Value}" Minimum="0" Maximum="{Binding MaximumImageIndex.Value}" TickFrequency="1" />
        <Image Source="{Binding CurrentImage.Value}" />
    </DockPanel>
</Window>
